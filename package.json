{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run --parallel start",
    "prep": "lerna clean && lerna bootstrap --hoist"
  },
  "devDependencies": {
    "lerna": "^3.11.0"
  }
}
