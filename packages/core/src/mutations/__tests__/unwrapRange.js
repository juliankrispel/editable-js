// @flow

describe('unwrapRange', () => {
  test('moves range of blocks before parent if selection contains top edge', () => {
  })
  test('moves range of blocks after parent if selection contains bottom edge', () => {
  })
  describe('when selection is in between edges', () => {
    test('splits parent block', () => {
    })
    test('moves blocks in between split parent blocks', () => {
    })
  })
})
